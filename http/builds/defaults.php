<?php
$os_name = "CentOS";             // OS distro
$os_version = "7.2.1511";        // OS version
$hardware = "virtual";           // Hardware type (virtual/metal)

// Options for virtual hardware
$lvm = 1;                        // Use LVM
$boot = 1;                       // Create a boot partition
$swap = 1;                       // Create a swap partition

// Options for metal hardware
$fqdn = "";                      // Full qualified name (metal)
$ipaddr = "";                    // IP address (metal)
?>
